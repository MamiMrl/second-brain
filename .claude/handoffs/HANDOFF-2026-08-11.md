# Handoff — 2026-08-11

**Focus:** Matt Pocock skills setup + agent configuration + handoff system redesign + auto mode

**Session date:** 2026-08-11  
**Status:** in-progress (setup complete, M3+ work pending)

---

## What Was Done This Session

### ✅ Completed

1. **Installed mattpocock-skills plugin globally**
   - Available across all 8 projects in `/Code/active/`
   - Configured for second-brain project specifically
   - All engineering skills now available: `/triage`, `/to-tickets`, `/diagnosing-bugs`, `/implement`, `/tdd`, `/code-review`, `/wayfinder`, `/ask-matt`, etc.

2. **Set up agent skills configuration for second-brain**
   - Created `CLAUDE.md` with Agent skills section
   - Configured issue tracker: GitHub Issues (PRs separate from triage queue)
   - Configured triage labels: defaults (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`)
   - Configured domain docs: single-context (reads `CONTEXT.md`, optional `docs/adr/`)
   - Created config files:
     - `docs/agents/issue-tracker.md` — GitHub workflow
     - `docs/agents/triage-labels.md` — label vocabulary
     - `docs/agents/domain.md` — domain doc consumer rules

3. **Redesigned handoff system**
   - **Old approach:** `/tmp/` temp files, lost/outdated, no tracking
   - **New approach:**
     - Persistent files: `.claude/handoffs/HANDOFF-YYYY-MM-DD.md`
     - Central registry: `.claude/handoffs/INDEX.md` (YAML format)
     - Task tracking: handoff references task IDs, auto-updates status based on task completion
     - Status tracking: `planned` / `in-progress` / `pending` / `completed`
     - Archive management: old handoffs marked archived after 30 days (not deleted)
   - Updated global handoff skill (`/Users/neu/.agents/skills/handoff/SKILL.md`) — applies to all projects

4. **Installed `/ask-matt` skill**
   - Project-level skill at `.claude/skills/ask-matt/`
   - Router/guide for navigating which skill to use for different situations
   - Explains main flow (idea → spec → tickets → implement) and on-ramps

5. **Configured auto mode globally**
   - Added `autoMode` settings to `~/.claude/settings.json`
   - Allowed (no prompts): file edits, npm commands, LangSmith API
   - Requires permission: git push, gh pr create, destructive bash operations
   - Applied to all projects; takes effect after Claude Code restart

6. **Created task list for next sessions**
   - Task #1: Complete M3 (query CLI integration & latency tuning)
   - Task #2: Implement M4 (Kindle ingestion parser + metadata)
   - Task #3: Build M5 (eval dataset & groundedness evaluator)
   - Task #4: Plan M6 (chat UI with conversation memory, v1.1)

---

## What Was NOT Completed (To-dos for Next Session)

### Critical path — M3 (Query CLI)
- [ ] **Task #1:** Complete M3: Query CLI integration & latency tuning
  - Integration testing with real queries on ingested documents
  - Edge case handling (empty retrieval, conflicting filters, malformed queries)
  - Latency tuning (target: <6s p50 end-to-end, <1s retrieval)
  - Verify "I don't know" gate works correctly (FR-3.3)
  - Test citations accuracy (FR-3.2)
  - **Current state:** filtering + existence routing + citations wired (per `f8bd22e` commit)
  - **Blocker:** M3 needs real integration testing before shipping

### M4 & beyond
- [ ] **Task #2:** Implement M4: Kindle ingestion (parser + metadata)
  - Parser for `My Clippings.txt` and HTML export formats (FR-1.4)
  - Metadata: book title, author, highlight date
  - Chunk-per-highlight strategy
  - Book-scoped queries
  - Error handling for unrecognized formats, encoding issues (FR-1.8)

- [ ] **Task #3:** Build M5: Eval dataset & groundedness evaluator
  - Author ~20–30 Q/A pairs (hybrid LLM-generate + human review, FR-5.4)
  - Three categories: positive/unknown/confirmed-absence (FR-5.4)
  - Store in `eval/dataset.jsonl` (source of truth, FR-5.5)
  - Sync to LangSmith Dataset
  - Run groundedness evaluator on eval set (FR-5.3)
  - Measure: retrieval hit rate (≥90%), hallucination rate (0), groundedness (≥95%)

- [ ] **Task #4:** Plan M6: Chat UI with conversation memory (v1.1)
  - Conversation memory: persist follow-up queries in session
  - Query rewriting for context
  - REPL or minimal web UI
  - Linked to session state
  - **Note:** Low priority, defer to v1.1. Start only after M5 eval is complete.

---

## Worth Keeping / Key Context

### Design frozen (locked per PRD grilling, July 20)
- See `README.md` §"Design decisions so far" (items 1–16)
- Two-level Document/Chunk model (MongoDB)
- Voyage AI `voyage-3.5` for embeddings
- Anthropic Citations API (not custom formatting)
- LLM-based metadata pre-filter + exhaustive scan for existence/negation
- Eval dataset: hybrid LLM-generate + human review, stored in `eval/dataset.jsonl`

### Files to review on next session
- `HANDOFF.md` — milestone status, next steps, useful links (created this session)
- `PRD.md` — complete product requirements + architecture
- `CONTEXT.md` — domain glossary (Document, Chunk, etc.)
- `README.md` — quick start, design decisions
- `docs/agents/` — agent skill configuration (issue tracker, triage, domain)

### Current git state
- Latest commit: `f8bd22e` — Wire generation with Anthropic's native Citations API (FR-3.1/3.2/3.4)
- Branch: `main`
- No uncommitted changes (this session created handoff/config files, not code changes)

### Setup that needs restart to activate
- ✅ **Auto mode** — configured in `~/.claude/settings.json`, takes effect after restart
- ✅ **`/ask-matt` skill** — installed at `.claude/skills/ask-matt/`, available after restart

---

## Suggested Skills for Next Session

- **`/ask-matt`** — Navigate which skill to use (route between triage, diagnosing-bugs, implement, tdd, etc.)
- **`/implement`** — Build M3 integration tests + M4 Kindle parser (use `/tdd` internally for test-first)
- **`/tdd`** — Test-first development for M3 edge cases + M4 parser
- **`/diagnosing-bugs`** — If M3 testing uncovers regressions
- **`/code-review`** — Review M3/M4 PRs before merging
- **`/wayfinder`** — If M5 eval dataset design needs navigation (decision map before implementation)

---

## Session Notes

- All PRD grilling branches resolved (Q1–Q9 confirmed 2026-07-20)
- User prefers extreme concision in chat (global CLAUDE.md) but complete session handoffs
- Handoff system redesign was driven by user feedback: temp files got lost, no task tracking, hard to resume
- New handoff approach: persistent INDEX.md + task-aware status tracking
- Matt Pocock skills + auto mode setup will improve workflow speed once Claude Code restarts

---

## Onboarding for Future Sessions

1. Read this handoff (you're reading it!)
2. Check `HANDOFF.md` for milestone status
3. Pick the top pending task from the list above
4. Reference `PRD.md` for functional requirements of that milestone
5. Use `/ask-matt` to route to the right skill for the work
6. Use `/handoff` with task IDs at end of session (new system tracks task progress)

